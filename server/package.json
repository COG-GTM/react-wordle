{
  "name": "wordle-versus-relay",
  "version": "0.1.0",
  "private": true,
  "description": "In-memory WebSocket relay for React Wordle versus mode",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test test",
    "check": "prettier --check src test",
    "fix": "prettier --write src test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.21.3"
  },
  "devDependencies": {
    "prettier": "2.6.1"
  }
}
